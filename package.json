{
  "name": "world-cup-prediction-league",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,green \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm install && npm run build --workspace=server && npm run build --workspace=client",
    "start": "npx prisma migrate deploy --schema=server/prisma/schema.prisma && npm run start --workspace=server",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc -p client/tsconfig.json --noEmit && tsc -p server/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "typescript": "^5.5.4"
  }
}
